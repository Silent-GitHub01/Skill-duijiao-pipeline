---
name: duijiao-pipeline
description: 双本古籍字符级对校流水线（抽文 → 分章经注分离 → 繁简折叠对齐 → 出校记 → 校订本 docx 与对比报告 html）。当用户给出「底本 + 参校本」两份同书文本（docx/epub/txt/azw3），要求校勘、对校、生成校订本、逐章校记、异文对比报告时调用。
agent_created: true
---

# 双本对校流水线（duijiao-pipeline）

把「底本 + 参校本」两份文献变成**校订本 + 逐章校记 + 异文对比报告**的可复现流水线。
方法论（该不该改字、证据够不够、句法之怪怎么读）走 `jiaokan-xiaofa` / `dushu-lizheng` / `gushu-wenli`；
本 Skill 只管**把这套学理落到数字文本上**。

## 何时触发
- 「以 X 为底本，校勘 Y」「生成校订本 / 逐章校记 / 异文对比报告」「两本对校」
- 用户给出两份以上同书文件，其中一本声明为底本。

## 环境
```
PY=C:\Users\Silen\.workbuddy\binaries\python\envs\default\Scripts\python.exe
# 依赖 zhconv（繁简折叠），其余标准库：$PY -m pip install zhconv
```

## 取文通道铁律（最容易踩的坑）
本地 `tencent-local-office-edit` MCP 是**编辑**通道：`doc_resolve_document_structure` 每段最多给
200 字 preview，**无法**导出逐字全文。字符级校勘必须用 `scripts/extract_docx.py` 直读
`word/document.xml`。成品 docx 仍走该 MCP（见第 5 步）。

## 五步流水线

### 1 取文
```
$PY scripts/extract_docx.py 底本.docx base_raw.txt
$PY scripts/extract_docx.py 参校本.docx src_raw.txt
```
一个 docx 段落 = 一行。先看前 25 行确认体例（章节标记、经文/注文标记）。

### 2 分章 + 经注分离（scripts/parse.py）
- 底本：`^第[一二三四五六七八九十百]+章$` 定章；正文以显式标记（如 `[經文]` / `[注解]`）分离。
- 参校本：`^(.{1,8}?)章第([一二三四五六七八九十]+)$` 定章；剔孤立数字行（页码残留）；
  `道经上／德经下` 之类的分篇标记直接跳过。
- **经／注判定**：以底本同章经文折叠串为锚，逐段算**覆盖率**
  `coverage(段, 底本经) = SequenceMatcher 全部匹配块总长 / len(段)`，≥ 0.60 判「经」，否则判「注」。
  比「最长公共子串」稳得多（能容忍句中一二字异文）。
- **现代噪声必先剔**：正则清「公众号／微信／扫码／http／联系…」等今人羼入文字；
  清洗后为空的段落**保留空占位**，否则段序漂移、`LABEL_OVERRIDE` 全乱。
- **人工裁定表** `LABEL_OVERRIDE = {章号: {段序: '经'|'注'}}`：阈值法必有两类误判——
  ① 短句带异文致覆盖度过低（例：「飂兮似无所止」只有 0.50 却确是经文）→ 需翻成「经」；
  ② 注文引经文致覆盖度过高（例：「道者，天地之始，岂得而名？」0.60 却是注）→ 需翻成「注」。
  脚本会打印「与奇偶先验不一致的章」与边界段（0.35≤cov≤0.80），**逐条人工过一遍**。
- 校验：源本经文应覆盖底本经文 ≥ 0.80 且顺序不乱；低于此者打印出来复核。

### 3 对齐（scripts/align.py）→ diffs.json + marks.json
- 折叠 = 繁→简（zhconv）+ 去标点；**先 `''.join(list)` 再 fold**（直接把 list 传给 zhconv 会静默出错，
  曾导致 80/81 章误判）。
- `difflib.SequenceMatcher` opcodes：`replace`→**异文**、`delete`→**脱文**、`insert`→**衍文**；
  相邻 delete/insert 互为**严格变位词**（sorted 相等，且核心长 ≥2）→ **倒文**。
- 去重：异文／倒文按（底本, 源本）；脱文／衍文按所脱所衍之**文字本身**（重出者并计 `n`）。
- `marks.json` 保留**逐处**的位置 `[begin,end)`（原串坐标），供对比报告高亮。

### 4 人工裁断（不可省）
通读 diffs，把机械对齐**判不出**的写进 `gen.py` 的 `SPECIAL` 特识表。必须人工裁断的典型：
- **羼经入注**：底本一整句经文，源本不别出为经，而见其注文之首/末 → 记特识，**两存待考**。
- **涉上而误复**（dittography）：源本一句与上句重出且文义不属 → 查源本自注是否引及该句，
  引及即可定为「涉上句而误复，夺某句」。此法屡验（《道德经》ch14）。
- **形近致讹**：源本误字（如 「況」讹「瓦」、「搏」讹「传」、「川谷」讹「川各」）。
- **真倒文**：机械层因换字而漏检的（如「持而保之／宝而持之」「弱之勝強，柔之勝剛」）。
- 源本羼入的今人广告：剔除，但**不出校记**，只在凡例声明。

### 5 出件（scripts/gen.py → md/html；docx 走 MCP）
```
$PY scripts/gen.py      # 出：校订本.md、逐章校记.md、对比报告.html
```
docx 生成走本地编辑通道（整本 5 万字一次插入仅 0.6 秒，**不必分块**）：
```
python3 edsdk.py call create_doc
python3 edsdk.py call doc_insert_markdown file_id=<id> idx=0 \
    markdown="file://C:/绝对路径/校订本.md"
python3 edsdk.py call save_file file_id=<id> file_path="C:\目标\校订本.docx"
```
注意 `markdown=` 支持 `file://<绝对路径>` 形式直接读文件，避免把长内容塞进参数。

## 输出体例
- **校订本**：每章 `【經文（校訂）】（底本读，如转简体须在凡例声明）` + `【原注】（源本注，照录不改）` + `【校記】`。
- **校记五目**：`異文：底本「A」，源本作「B」。` / `脫文：底本「…」，源本無「X」。` /
  `衍文：源本「…」，底本無「X」。` / `倒文：底本「A」，源本作「B」。` / `特識：…`（须具说者，附定案强度）。
- **对比报告 HTML**：逐章左右对照表，红=异文、橙=衍、蓝=脱〔脫〕、紫=倒；附校记；
  避讳若两本正文均无实例，**不列讳例**（「讳字表仅供参照，必以正文实有实例为准」）。

## 铁律
1. 异体／繁简折叠**只用于比对**，绝不改原文一字。
2. 不校改底本，只出校记（存真原则）；确需改处须标依据。
3. 讲不通先疑文本、次疑旧注、最后疑己；证据不足**两存待考**，不强作说通。
4. 机械对齐只是「校异同」，**是非判断必须人工**——凡下断语必注明致误之由与证据路数。
5. 全流程保留可复现性：脚本 + 中间产物（raw/parsed/diffs/marks）一并留档。

## 参考
- `references/pitfalls.md`：实战踩坑与阈值调参记录（《道德经》王弼本 × 宋徽宗御解全 81 章实例）。
- `scripts/`：extract_docx.py / parse.py / align.py / gen.py（按固定文件名在同一工作目录内串行运行）。
